import React from 'react';import { useNavigate, useLocation } from 'react-router-dom';const ChatWidget = () => {    const navigate = useNavigate();    const location = useLocation();    if (location.pathname === '/chat') return null;    return (        <button            onClick={() => navigate('/chat')}            className="fixed bottom-6 right-6 z-[60] group flex items-center justify-center w-16 h-16 rounded-full bg-brand-primary text-white shadow-[0_0_20px_rgba(0,240,255,0.6)] hover:shadow-[0_0_30px_rgba(0,240,255,0.8)] hover:scale-110 transition-all duration-300 border border-white/20 animate-bounce-slow"            title="Open Mission Assistant"        >            <span className="text-3xl animate-pulse">🤖</span>            <div className="absolute right-16 px-3 py-1 rounded bg-black/80 backdrop-blur border border-white/10 text-xs font-mono text-brand-primary opacity-0 group-hover:opacity-100 transition-opacity whitespace-nowrap pointer-events-none">                AI ASSISTANT            </div>        </button>    );};export default ChatWidget;