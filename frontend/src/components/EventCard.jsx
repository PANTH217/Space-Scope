import React from 'react';const EventCard = ({ event, compact = false }) => {  const formatDate = (dateString) => {    return new Date(dateString).toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric' });  };  return (    <div className={`group relative overflow-hidden ${compact ? 'py-2' : 'glass-card p-6'}`}>      <div className="flex justify-between items-start">        <div className="flex-1">          <div className="flex items-center space-x-2 mb-1">            <span className="text-brand-secondary text-xs font-bold uppercase tracking-wide border border-brand-secondary/30 px-1.5 py-0.5 rounded">              {event.type}            </span>            <span className="text-gray-400 text-xs font-mono">{formatDate(event.date)}</span>          </div>          <h3 className={`font-display font-bold text-white group-hover:text-brand-primary transition-colors ${compact ? 'text-sm' : 'text-lg'}`}>            {event.title}          </h3>          {!compact && (            <p className="text-gray-300 text-sm mt-2 leading-relaxed">              {event.description}            </p>          )}        </div>        {!compact && (          <button className="ml-4 p-2 rounded-full bg-white/5 hover:bg-brand-primary/20 text-gray-400 hover:text-brand-primary transition-all">            🔔          </button>        )}      </div>      {!compact && (        <div className="mt-4 pt-4 border-t border-white/5 flex justify-between items-center text-xs text-gray-500">          <span>Target: {event.visibility}</span>          <span>Best Time: {event.bestViewingTime}</span>        </div>      )}    </div>  );};export default EventCard;