import React, { useState, useEffect } from 'react';const SettingsModal = ({ isOpen, onClose }) => {    const [nasaKey, setNasaKey] = useState('');    const [geminiKey, setGeminiKey] = useState('');    const [showSuccess, setShowSuccess] = useState(false);    useEffect(() => {        if (isOpen) {            setNasaKey(localStorage.getItem('nasa_api_key') || '');            setGeminiKey(localStorage.getItem('gemini_api_key') || '');        }    }, [isOpen]);    const handleSave = () => {        localStorage.setItem('nasa_api_key', nasaKey);        localStorage.setItem('gemini_api_key', geminiKey);        setShowSuccess(true);        setTimeout(() => {            setShowSuccess(false);            onClose();            window.location.reload();         }, 1500);    };    if (!isOpen) return null;    return (        <div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/80 backdrop-blur-sm p-4 animate-in fade-in duration-200">            <div className="glass-card w-full max-w-md p-8 relative">                <button                    onClick={onClose}                    className="absolute top-4 right-4 text-gray-500 hover:text-white"                >                    ✕                </button>                <h2 className="text-2xl font-display font-bold text-white mb-6 flex items-center gap-2">                    <span>⚙️</span> Global Settings                </h2>                <div className="space-y-6">                    <div>                        <label className="block text-brand-primary text-xs uppercase tracking-widest mb-2 font-bold">NASA API Key</label>                        <p className="text-xs text-gray-400 mb-2">Required for APOD, Earth Impact, and Space Weather.</p>                        <input                            type="password"                            placeholder="Enter NASA API Key"                            value={nasaKey}                            onChange={(e) => setNasaKey(e.target.value)}                            className="w-full bg-black/50 border border-white/20 rounded-lg p-3 text-white focus:border-brand-primary focus:outline-none font-mono text-sm"                        />                        <a href="https://api.nasa.gov/" target="_blank" rel="noreferrer" className="text-[10px] text-gray-500 hover:text-white underline mt-1 inline-block">                            Get free key from api.nasa.gov                        </a>                    </div>                    <div>                        <label className="block text-brand-primary text-xs uppercase tracking-widest mb-2 font-bold">Gemini API Key</label>                        <p className="text-xs text-gray-400 mb-2">Required for the AI Mission Assistant.</p>                        <input                            type="password"                            placeholder="Enter Google Gemini API Key"                            value={geminiKey}                            onChange={(e) => setGeminiKey(e.target.value)}                            className="w-full bg-black/50 border border-white/20 rounded-lg p-3 text-white focus:border-brand-primary focus:outline-none font-mono text-sm"                        />                        <a href="https://aistudio.google.com/app/apikey" target="_blank" rel="noreferrer" className="text-[10px] text-gray-500 hover:text-white underline mt-1 inline-block">                            Get key from Google AI Studio                        </a>                    </div>                </div>                <div className="mt-8 pt-6 border-t border-white/10 flex justify-end gap-4">                    <button                        onClick={onClose}                        className="px-4 py-2 text-sm text-gray-400 hover:text-white transition-colors"                    >                        Cancel                    </button>                    <button                        onClick={handleSave}                        className={`glass-button flex items-center gap-2 ${showSuccess ? 'bg-green-500/20 border-green-500 text-green-400' : ''}`}                    >                        {showSuccess ? (                            <><span>✓</span> Saved!</>                        ) : (                            'Save Configuration'                        )}                    </button>                </div>            </div>        </div>    );};export default SettingsModal;