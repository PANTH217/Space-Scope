import React, { useState, useEffect } from 'react';import { getDailyFacts } from '../services/nasaApi';const FactCarousel = () => {    const [fact, setFact] = useState('');    const [isAnimating, setIsAnimating] = useState(false);    useEffect(() => {        setFact(getDailyFacts());    }, []);    const handleNext = () => {        setIsAnimating(true);        setTimeout(() => {            setFact(getDailyFacts());            setIsAnimating(false);        }, 500);    };    return (        <div className="glass-card p-6 relative overflow-hidden group min-h-[200px] flex flex-col justify-center">            <div className="absolute top-0 right-0 w-24 h-24 bg-brand-primary/10 rounded-full blur-3xl group-hover:bg-brand-primary/20 transition-all"></div>            <div className="flex justify-between items-start mb-4">                <h3 className="font-display font-bold text-white uppercase tracking-wider text-sm flex items-center gap-2">                    <span className="text-xl">💡</span> Did You Know?                </h3>            </div>            <div className={`transition-opacity duration-500 ${isAnimating ? 'opacity-0' : 'opacity-100'}`}>                <p className="text-lg md:text-xl text-gray-200 font-light leading-relaxed">                    "{fact}"                </p>            </div>            <div className="mt-4 flex justify-end">                <div className="h-1 w-12 bg-white/20 rounded-full overflow-hidden">                    <div className="h-full bg-brand-primary w-1/3 animate-[pulse_3s_infinite]"></div>                </div>            </div>        </div>    );};export default FactCarousel;