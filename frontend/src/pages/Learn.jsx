import React, { useState } from 'react';import { useNavigate } from 'react-router-dom';import { academyModules } from '../data/spaceAcademyData';import { FaPlay, FaGraduationCap, FaSearch, FaAtom } from 'react-icons/fa';const Learn = () => {  const navigate = useNavigate();  const [searchTerm, setSearchTerm] = useState('');  const [activeCategory, setActiveCategory] = useState('All');  const categories = ['All', ...new Set(academyModules.map(m => m.category))];  const filteredModules = academyModules.filter(module => {    const matchesSearch = module.title.toLowerCase().includes(searchTerm.toLowerCase()) ||      module.category.toLowerCase().includes(searchTerm.toLowerCase());    const matchesCategory = activeCategory === 'All' || module.category === activeCategory;    return matchesSearch && matchesCategory;  });  return (    <div className="animate-[fadeIn_0.5s_ease-out] min-h-screen pb-10">      <div className="relative rounded-3xl overflow-hidden mb-12 border border-brand-primary/20 shadow-[0_0_50px_rgba(0,240,255,0.1)]">        <div className="absolute inset-0 bg-gradient-to-r from-black via-black/80 to-transparent z-10"></div>        <img          src="https://images.unsplash.com/photo-1451187580459-43490279c0fa?auto=format&fit=crop&q=80"          alt="Academy Hero"          className="w-full h-80 object-cover"        />        <div className="absolute inset-0 z-20 flex flex-col justify-center px-10">          <h1 className="text-5xl font-display font-bold text-transparent bg-clip-text bg-gradient-to-r from-brand-primary to-white mb-4 drop-shadow-[0_0_10px_rgba(0,240,255,0.5)]">            Space Academy          </h1>          <p className="text-xl text-gray-300 max-w-2xl font-light">            Master the cosmos. Access our classified library of 100+ interactive modules, video briefings, and certification exams.          </p>        </div>      </div>      <div className="flex flex-col md:flex-row justify-between items-center gap-6 mb-10">        <div className="flex gap-2 overflow-x-auto pb-2 w-full md:w-auto custom-scrollbar">          {categories.map(cat => (            <button              key={cat}              onClick={() => setActiveCategory(cat)}              className={`px-6 py-2 rounded-full border transition-all whitespace-nowrap font-mono text-sm tracking-wider ${activeCategory === cat                ? 'bg-brand-primary text-black border-brand-primary font-bold shadow-[0_0_15px_rgba(0,240,255,0.4)]'                : 'bg-black/40 text-gray-400 border-white/10 hover:border-brand-primary/50 hover:text-white'                }`}            >              {cat.toUpperCase()}            </button>          ))}        </div>        <div className="relative w-full md:w-96">          <FaSearch className="absolute left-4 top-1/2 -translate-y-1/2 text-brand-primary" />          <input            type="text"            placeholder="Search database..."            value={searchTerm}            onChange={(e) => setSearchTerm(e.target.value)}            className="w-full bg-black/40 border border-white/20 rounded-full py-3 pl-12 pr-6 text-white text-sm focus:outline-none focus:border-brand-primary focus:shadow-[0_0_15px_rgba(0,240,255,0.2)] transition-all font-mono"          />        </div>      </div>      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">        {filteredModules.map((module) => (          <div            key={module.id}            onClick={() => navigate(`/learn/${module.id}`)}            className="group relative bg-black/40 backdrop-blur-md border border-white/10 rounded-2xl overflow-hidden hover:border-brand-primary/60 transition-all duration-500 cursor-pointer hover:shadow-[0_0_30px_rgba(0,240,255,0.15)]"          >            <div className="h-48 overflow-hidden relative">              <div className="absolute inset-0 bg-brand-primary/10 group-hover:bg-transparent transition-all z-10"></div>              <img                src={module.thumbnail}                alt={module.title}                className="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-700"                onError={(e) => {                  e.target.onerror = null;                  e.target.src = 'https://upload.wikimedia.org/wikipedia/commons/thumb/0/0d/Hubble_ultra_deep_field_high_rez_edit1.jpg/800px-Hubble_ultra_deep_field_high_rez_edit1.jpg';                 }}              />              <div className="absolute top-3 right-3 z-20 bg-black/80 backdrop-blur-md border border-brand-primary/30 px-3 py-1 rounded-full">                <span className="text-xs font-mono text-brand-primary uppercase tracking-wider">{module.difficulty}</span>              </div>            </div>            <div className="p-6">              <div className="flex items-center gap-2 mb-3">                <FaAtom className="text-brand-secondary text-sm" />                <span className="text-xs text-gray-400 uppercase tracking-widest">{module.category}</span>              </div>              <h3 className="text-xl font-bold font-display text-white mb-2 group-hover:text-brand-primary transition-colors">                {module.title}              </h3>              <p className="text-gray-400 text-sm line-clamp-2 mb-6 font-light">                {module.description}              </p>              <div className="flex items-center justify-between border-t border-white/10 pt-4">                <div className="flex items-center gap-2 text-xs font-mono text-gray-500">                  <FaGraduationCap />                  <span>{module.questions.length} QUESTIONS</span>                </div>                <button className="flex items-center gap-2 text-brand-primary text-sm font-bold tracking-wider group-hover:translate-x-1 transition-transform">                  INITIALIZE <FaPlay size={10} />                </button>              </div>            </div>          </div>        ))}      </div>    </div>  );};export default Learn;