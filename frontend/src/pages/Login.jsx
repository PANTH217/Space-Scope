import React, { useState } from 'react';import { useAuth } from '../contexts/AuthContext';import { useNavigate } from 'react-router-dom';import SolarSystemBackground from '../components/SolarSystemBackground';import logo from '../assets/logo.png';const Login = () => {    const [isLogin, setIsLogin] = useState(true);    const [email, setEmail] = useState('');    const [password, setPassword] = useState('');    const [error, setError] = useState('');    const [message, setMessage] = useState('');    const [loading, setLoading] = useState(false);    const { login, signup, loginWithGoogle, logout } = useAuth();    const navigate = useNavigate();    const handleSubmit = async (e) => {        e.preventDefault();        setError('');        setMessage('');        setLoading(true);        try {            if (isLogin) {                await login(email, password);            } else {                const username = email.split('@')[0];                await signup(username, email, password);                setMessage("Registration successful! You may now log in.");                setIsLogin(true);                setPassword('');                setLoading(false);                return;            }            navigate('/');        } catch (err) {            console.error("Auth Error:", err);            let msg = "Authentication Failed";            if (err.code === 'auth/invalid-credential' || err.code === 'auth/wrong-password') {                msg = "Access Denied: Invalid Credentials.";            } else if (err.code === 'auth/user-not-found') {                msg = "User not found. Please Register.";            } else if (err.code === 'auth/email-already-in-use') {                msg = "Account exists. Please log in.";            } else if (err.code === 'auth/weak-password') {                msg = "Password too weak.";            } else {                msg = err.message || 'An unexpected error occurred';            }            setError(msg);        } finally {            setLoading(false);        }    };    const handleGoogleLogin = async () => {        try {            setError("");            setMessage("");            setLoading(true);            await loginWithGoogle();            navigate("/");        } catch (err) {            console.error("Google Auth Error:", err);            setError("Google Sign In Failed.");        } finally {            setLoading(false);        }    };    return (        <div className="relative min-h-screen w-full overflow-hidden flex items-center justify-end pr-4 md:pr-20">            <SolarSystemBackground />            <div className="relative z-10 w-full max-w-md px-4">                <div className="bg-black/30 backdrop-blur-xl border border-white/10 rounded-2xl p-8 shadow-[0_0_100px_rgba(0,0,0,0.8)] animate-[fadeIn_1s_ease-out]">                    <div className="text-center mb-6">                        <div className="inline-block mb-4 transition-transform hover:scale-105 duration-500">                            <img src={logo} alt="SpaceScope" className="w-24 h-24 object-contain drop-shadow-[0_0_15px_rgba(0,240,255,0.5)]" />                        </div>                        <h2 className="text-3xl font-display font-bold tracking-wider text-white mb-2">                            {isLogin ? 'MISSION CONTROL' : 'JOIN THE FLEET'}                        </h2>                        <p className="font-mono text-sm tracking-[0.2em] text-brand-primary uppercase">                            {isLogin ? 'Identity Verification' : 'New Cadet Registration'}                        </p>                    </div>                    <div className="flex mb-6 bg-black/40 p-1 rounded-lg border border-white/10">                        <button                            type="button"                            className={`flex-1 py-2 rounded-md font-mono text-sm tracking-widest transition-colors ${isLogin ? 'bg-brand-primary text-white' : 'text-gray-400 hover:text-white'}`}                            onClick={() => { setIsLogin(true); setError(''); setMessage(''); }}                        >                            LOGIN                        </button>                        <button                            type="button"                            className={`flex-1 py-2 rounded-md font-mono text-sm tracking-widest transition-colors ${!isLogin ? 'bg-brand-primary text-white' : 'text-gray-400 hover:text-white'}`}                            onClick={() => { setIsLogin(false); setError(''); setMessage(''); }}                        >                            REGISTER                        </button>                    </div>                    {error && (                        <div className="mb-6 p-3 bg-red-500/20 border border-red-500/50 rounded text-red-200 text-sm text-center font-mono animate-[fadeIn_0.3s_ease-out]">                            ⚠️ {error}                        </div>                    )}                    {message && (                        <div className="mb-6 p-3 bg-green-500/20 border border-green-500/50 rounded text-green-200 text-sm text-center font-mono animate-[fadeIn_0.3s_ease-out]">                            ✓ {message}                        </div>                    )}                    <form onSubmit={handleSubmit} className="space-y-6">                        <div>                            <input                                type="email"                                required                                className="w-full bg-black/40 border border-white/10 rounded-lg px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:border-brand-primary focus:bg-black/60 transition-all font-mono text-sm"                                placeholder="COMMANDER ID (EMAIL)"                                value={email}                                onChange={(e) => setEmail(e.target.value)}                            />                        </div>                        <div>                            <input                                type="password"                                required                                className="w-full bg-black/40 border border-white/10 rounded-lg px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:border-brand-primary focus:bg-black/60 transition-all font-mono text-sm"                                placeholder="ACCESS CODE (PASSWORD)"                                value={password}                                onChange={(e) => setPassword(e.target.value)}                            />                        </div>                        <div className="space-y-4 pt-2">                            <button                                disabled={loading}                                className="w-full bg-brand-primary/80 hover:bg-brand-primary text-white font-bold py-3 rounded-lg uppercase tracking-widest transition-all duration-300 shadow-[0_0_20px_rgba(0,240,255,0.3)] disabled:opacity-50 disabled:cursor-not-allowed text-sm"                            >                                {loading ? 'PROCESSING...' : (isLogin ? 'INITIATE LAUNCH' : 'SUBMIT APPLICATION')}                            </button>                            <div className="relative flex items-center gap-4 py-2 opacity-50">                                <div className="h-px bg-white/20 flex-1"></div>                                <span className="text-[10px] text-white font-mono uppercase">OR</span>                                <div className="h-px bg-white/20 flex-1"></div>                            </div>                            <button                                type="button"                                onClick={handleGoogleLogin}                                disabled={loading}                                className="w-full bg-white/10 hover:bg-white/20 text-white font-bold py-3 rounded-lg flex items-center justify-center gap-3 transition-all duration-300 disabled:opacity-50 text-sm uppercase tracking-wider border border-white/10"                            >                                <img src="https://www.gstatic.com/firebasejs/ui/2.0.0/images/auth/google.svg" alt="Google" className="w-4 h-4" />                                Sign in with Google                            </button>                        </div>                    </form>                </div>            </div>        </div>    );};export default Login;